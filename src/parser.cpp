#include <cstddef>
#include <cstdint>
#include <iostream>
#include <vector>

#include "parser.hpp"
#include "token.hpp"


std::vector<Token> tokenParser(std::vector<Token> tokens) {
    return tokens;
}
